'use client';

/**
 * The screen hooks live in @yugo/app-core so web and mobile share one
 * implementation. This module re-exports them behind the 'use client'
 * boundary the App Router expects.
 */
export * from '@yugo/app-core';
